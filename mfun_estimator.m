function p_param = mfun_estimator(data, pre)
% MATLAB Function for parameter estimator 
%
% Its implementation can be found in "impl_estimator" 

    %p_param = gaussian_timeseries(data, pre);

    p_param = gaussian_estimator(data,pre);

end
